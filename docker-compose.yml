services:
  stats-server:
    build:
      context: .
      dockerfile: stat/server/Dockerfile
    ports:
      - "9090:9090"

  service:
    build:
      context: ./service
    ports:
      - "8080:8080"